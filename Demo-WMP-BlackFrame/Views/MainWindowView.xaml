<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:vm="clr-namespace:Demo_WMP_BlackFrame.ViewModels">
	<DataTemplate x:Key="MainWindowViewTemplate" DataType="{x:Type vm:MainWindowViewModel}">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>

			<TextBlock Text="Video Player Kinds" TextAlignment="Center" />
			<ComboBox Grid.Row="1" ItemsSource="{Binding VideoPlayerKinds}" SelectedItem="{Binding SelectedKind}" MaxWidth="150">
				<ComboBox.ItemTemplate>
					<DataTemplate>
						<TextBlock Text="{Binding Name}" />
					</DataTemplate>
				</ComboBox.ItemTemplate>
			</ComboBox>
			<ContentPresenter Grid.Row="2" x:Name="VideoPlayerPresenter" Content="{Binding VideoPlayerViewModel}" />
		</Grid>
	</DataTemplate>
</ResourceDictionary>